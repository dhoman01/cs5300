CONST c = true;
      d = false;

VAR a,b :integer;
    e   : boolean;

BEGIN

a := 1;
b := 2;
$write(a,b);

a := a + b; $a=3
b := a + b; $b=5
$write(a,b);

a := a * b; $a=15
b := a * b; $b=75
$write(a,b);

e := c | d;
e := a > b;
e := a < b;
e := a = b;
e := a <> b;

END.